<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abajin.innovation.mapper.TicketStatusMapper">
    <resultMap id="BaseResultMap" type="com.abajin.innovation.entity.TicketStatus">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="description" property="description"/>
        <result column="is_system" property="isSystem"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM ticket_status ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM ticket_status WHERE id = #{id}
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        SELECT * FROM ticket_status WHERE name = #{name}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ticket_status (name, sort_order, description, is_system, create_time, update_time)
        VALUES (#{name}, #{sortOrder}, #{description}, #{isSystem}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        UPDATE ticket_status
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="description != null">description = #{description},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ticket_status WHERE id = #{id}
    </delete>

    <select id="countTicketsByStatusId" resultType="int">
        SELECT COUNT(*) FROM ticket WHERE status_id = #{statusId} AND is_deleted = 0
    </select>
</mapper>
